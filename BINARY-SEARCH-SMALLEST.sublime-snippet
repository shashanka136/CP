<snippet>
	<content><![CDATA[
	//smallest>= ${1:x}
	int l=-1; //a[l] < ${1:x}
	int r = n,mid; // a[r] >= ${1:x}
	while(r > l+1){
		mid = l+r>>1;
		if(a[mid] >= ${1:x}){
			r = mid;
		}else{
			l = mid;
		}
	}
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>_binary_search_smallest--</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<!-- <scope>source.python</scope> -->
</snippet>
